<?xml version="1.0" encoding="utf-8"?>
<resources>

    <integer name="ga_sessionTimeout">300</integer>

    <!-- Enable automatic Activity measurement -->
    <bool name="ga_autoActivityTracking">true</bool>

    <!-- Percentage of events to include in reports -->
    <string name="ga_sampleFrequency">100.0</string>

    <!-- catch and report uncaught exceptions from the app -->
    <bool name="ga_reportUncaughtExceptions">true</bool>

    <!-- How long a session exists before giving up -->
    <integer name="ga_sessionTimeout">-1</integer>

    <!-- the Local LogLevel for Analytics -->
    <string name="ga_logLevel">verbose</string>

    <!-- how often the dispatcher should fire -->
    <integer name="ga_dispatchPeriod">30</integer>

    <!-- Treat events as test events and don't send to google -->
    <bool name="ga_dryRun">false</bool>

    <!-- The screen names that will appear in reports -->
    <screenName name="menu">Menu</screenName>
    <screenName name="game">Game</screenName>
    <screenName name="game_over">Game Over</screenName>
    <screenName name="top_scores">Top scores</screenName>
    <screenName name="rate">Rate</screenName>
    <screenName name="interstitial_ad">Interstitial Ad</screenName>
    <screenName name="interstitial_clicked">Interstitial Ad clicked</screenName>
    <screenName name="banner_ad_clicked">Banner Ad clicked</screenName>

    <string name="ga_trackingId">UA-47894554-3</string>

</resources>